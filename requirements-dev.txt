# Herramientas para DESARROLLAR y probar. NO hacen falta para usar el
# programa: el contador solo instala lo de requirements.txt.
#
# Instalar con:
#   .venv/bin/python -m pip install -r requirements-dev.txt
#   .venv/bin/python -m playwright install chromium

# Abre un navegador de verdad para revisar que las pantallas funcionen.
playwright

# Revisa que no haya nombres sin definir ni imports olvidados. Se volvió
# útil cuando la API se partió en varios archivos: un import que falta no
# revienta al arrancar, revienta cuando alguien entra a esa pantalla.
pyflakes
